== Система LaTeX

LaTeX (произносится "латех" или "лэйтех")~--- система подготовки документов, построенная поверх языка TeX, разработанного Дональдом Кнутом в 1978~году @knuth1984texbook. Лесли Лэмпорт создал LaTeX в 1984~году как набор макросов, облегчающих использование TeX. Сегодня актуальной версией является LaTeX2e, а в разработке находится LaTeX3 @latex-project.

Среди ключевых особенностей системы можно выделить следующие.

- *Мощный набор математических формул.* LaTeX является стандартом для публикации научных статей с формулами~--- пакеты `amsmath`, `amssymb` и  `amsthm` предоставляют богатый инструментарий.
- *Разделение содержания и оформления.* Автор пишет текст с разметкой, а система берёт на себя расстановку переносов, выравнивание и нумерацию.
- *Огромная экосистема.* Репозиторий CTAN содержит более 6000 пакетов для решения задач от вставки кода @minted-ctan до построения диаграмм.
- *Стабильность.* Документы, написанные 20 лет назад, как правило, и сегодня компилируются корректно.

К недостаткам LaTeX относятся: многословный синтаксис, сложная диагностика ошибок, а также необходимость нескольких запусков компилятора для корректного формирования ссылок и оглавления. Рекомендуемое справочное издание по системе~--- книга "The LaTeX Companion" @mittelbach2004latex.

== Система Typst

Typst~--- новая система вёрстки документов, разработанная Лауренцем Мэдье (Laurenz Mädje) и Мартином Хаугом (Martin Haug) @typst-github. Публичная бета-версия вышла в марте 2023~года @typst-blog-2023. Система написана на языке Rust, что обеспечивает высокую производительность и надёжность.

Typst использует собственный язык разметки, сочетающий простой синтаксис для текста с полноценным языком программирования для сложной логики. Компилятор работает в инкрементальном режиме: при изменении одной части документа повторно обрабатывается только она, что даёт мгновенный предварительный просмотр @typst-docs.

Основные преимущества Typst:
- *Читаемый синтаксис.* Заголовок задаётся через `= Заголовок`, жирный текст~--- через `*текст*`, что ближе к Markdown.
- *Инкрементальная компиляция.* Изменение одной страницы не требует перекомпиляции всего документа.
- *Понятные ошибки.* Сообщения об ошибках содержат точное указание строки и понятное описание проблемы.
- *Встроенный язык программирования.* Скриптование доступно прямо в разметке без дополнительных пакетов.
- *Современный менеджер пакетов.* Пакеты подключаются и кешируются автоматически.
