> [!INFO]
> В папке typst, я дополнительно реализовал предложенную задачу на Typst, потому что я твердо убежден: мы должны перестать использовать LaTeX и переходить на Typst прямо сейчас

LaTeX файлы лежат в одноименной папки каждого задания

> [!NOTE]
> Поскольку в Typst нет жестко заданных классов документов (`article`, `book` и т.д.), в Задании 2 представлен только один файл Typst со стилями по умолчанию.

Компиляция LaTeX файлов для заданий `task1`, `task2` происходит командой `latexmk <path to main LaTeX file>`
Компиляция Typst файлов для заданий `task1`, `task2` происходит командой `typst compile <path to main Typst file>`

- `task1` - задание на форматирование текста 
- `task2` - задание на набор математических формул
